import sys
import subprocess
import os

print("🔧 กำลังเริ่มกระบวนการซ่อมแซมระบบ... (Repairing...)")
print(f"📍 กำลังติดตั้งลงที่: {sys.executable}")

# รายชื่อเครื่องมือที่ต้องใช้
libraries = ["google-generativeai", "streamlit", "PyPDF2", "python-docx"]

# วนลูปติดตั้งทีละตัว แบบระบุพิกัดแม่นยำ
for lib in libraries:
    try:
        print(f"⏳ กำลังติดตั้ง: {lib}...")
        subprocess.check_call([sys.executable, "-m", "pip", "install", lib])
        print(f"✅ ติดตั้ง {lib} สำเร็จ!")
    except Exception as e:
        print(f"❌ ติดตั้ง {lib} ไม่สำเร็จ: {e}")

print("\n" + "="*50)
print("🎉 ซ่อมแซมเสร็จสิ้น! ครูสามารถกลับไปรันไฟล์งานได้เลยครับ")
print("="*50)