# วิธีการตั้งค่า Google Gemini API Key

## 🔒 วิธีที่ 1: ใช้ไฟล์ .env (แนะนำสำหรับ Local Development)

1. **คัดลอกไฟล์ตัวอย่าง:**
   ```bash
   copy .env.example .env
   ```

2. **แก้ไขไฟล์ `.env`:**
   ```
   GOOGLE_GEMINI_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXX
   ```

3. **รีสตาร์ท Streamlit:**
   ```bash
   streamlit run app_launcher.py
   ```

## 🌐 วิธีที่ 2: ใช้ Streamlit Secrets (สำหรับ Streamlit Cloud)

1. **สร้างโฟลเดอร์:**
   ```bash
   mkdir .streamlit
   ```

2. **สร้างไฟล์ `.streamlit/secrets.toml`:**
   ```toml
   GOOGLE_GEMINI_API_KEY = "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXX"
   ```

3. **Deploy บน Streamlit Cloud:**
   - ไปที่ Settings → Secrets
   - เพิ่ม secrets เดียวกัน

## 💻 วิธีที่ 3: ใส่ชั่วคราวผ่านหน้าเว็บ

- คลิกที่ "🔑 ตั้งค่า Google Gemini API Key"
- ใส่ API Key (จะหายเมื่อปิดเบราว์เซอร์)

## 🔑 วิธีขอ API Key ฟรี

1. ไปที่: https://makersuite.google.com/app/apikey
2. คลิก "Create API Key"
3. เลือก Project (หรือสร้างใหม่)
4. คัดลอก API Key

## ⚠️ ข้อควรระวัง

- **ห้าม** commit ไฟล์ `.env` ลง Git
- **ห้าม** แชร์ API Key ให้ใครเห็น
- API Key ฟรีมีโควต้าจำกัด (15 requests/minute)
