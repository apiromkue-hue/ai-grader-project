# 🚨 ปัญหา: เว็บไซต์ไม่แสดงผล

**URL:** https://project-ai.triamudomsouth.ac.th/
**วันที่:** 23 ธันวาคม 2025

---

## 🔍 สาเหตุ

Server กำลังรันไฟล์เก่า **`student_view.py`** 
แต่ตอนนี้เราได้สร้างหน้าหลักใหม่เป็น **`Home.py`** แล้ว

---

## ✅ วิธีแก้ไข (เลือก 1 วิธี)

### 🎯 วิธีที่ 1: ใช้สคริปต์อัตโนมัติ (แนะนำ)

1. อัปโหลดไฟล์ทั้งหมดขึ้น server
2. SSH เข้า server:
   ```bash
   ssh user@project-ai.triamudomsouth.ac.th
   ```
3. เข้าโฟลเดอร์โปรเจค:
   ```bash
   cd /var/www/Project_AI_Grader
   ```
4. ให้สิทธิ์รันสคริปต์:
   ```bash
   chmod +x update_streamlit.sh
   ```
5. รันสคริปต์:
   ```bash
   ./update_streamlit.sh
   ```

**เท่านี้เสร็จแล้ว!** ✅

---

### 🛠️ วิธีที่ 2: แก้ไขด้วยตัวเอง (Manual)

```bash
# 1. SSH เข้า server
ssh user@project-ai.triamudomsouth.ac.th

# 2. หยุด Streamlit เดิม
pkill -f streamlit

# 3. เข้าโฟลเดอร์
cd /var/www/Project_AI_Grader

# 4. เปิด virtual environment
source venv/bin/activate

# 5. รัน Home.py ใหม่
nohup streamlit run Home.py --server.port=8501 --server.address=0.0.0.0 > streamlit.log 2>&1 &

# 6. ตรวจสอบ
ps aux | grep streamlit
tail -f streamlit.log
```

---

### 🔧 วิธีที่ 3: แก้ไข systemd service (ถ้ามี)

```bash
# 1. หยุด service
sudo systemctl stop streamlit-ai-grader

# 2. แก้ไข config
sudo nano /etc/systemd/system/streamlit-ai-grader.service

# 3. เปลี่ยนบรรทัดนี้:
#    จาก: streamlit run student_view.py
#    เป็น: streamlit run Home.py

# 4. Reload และเริ่มใหม่
sudo systemctl daemon-reload
sudo systemctl start streamlit-ai-grader
sudo systemctl status streamlit-ai-grader
```

---

## 📦 ไฟล์ที่ต้องอัปโหลดขึ้น Server

อัปโหลดไฟล์เหล่านี้ไปแทนที่บน server:

### ไฟล์หลัก (จำเป็น):
1. ✅ **Home.py** - หน้าแรกใหม่
2. ✅ **pages/2_Admin_Panel.py** - Admin Panel ที่แก้ไขแล้ว
3. ✅ **Procfile** - อัปเดตให้รัน Home.py
4. ✅ **Dockerfile** - อัปเดตให้รัน Home.py

### ไฟล์เสริม (ช่วยแก้ไข):
5. ✅ **update_streamlit.sh** - สคริปต์อัปเดตอัตโนมัติ
6. ✅ **test_deployment.py** - ไฟล์ทดสอบระบบ
7. ✅ **UPDATE_SERVER.md** - คู่มือแก้ไขละเอียด

---

## 🧪 ทดสอบว่าแก้ไขสำเร็จ

### ตรวจสอบบน Server:
```bash
# ดูว่า streamlit รันไฟล์อะไร
ps aux | grep streamlit

# ต้องเห็น: streamlit run Home.py
```

### ตรวจสอบบน Browser:
1. เปิด: https://project-ai.triamudomsouth.ac.th/
2. ต้องเห็น:
   - ✨ SVG icon (ไม่ใช่ emoji)
   - 📦 Grid cards 6 ฟีเจอร์
   - 📖 คู่มือการใช้งาน 2 cards แนวนอน
   - 👥 Footer อยู่กึ่งกลาง
   - 🎨 สีชมพู-ม่วง K-Minimal design

---

## 🔧 การแก้ไขปัญหาเพิ่มเติม

### ถ้าเว็บยังไม่แสดงผล:

#### 1. ตรวจสอบ Log
```bash
tail -100 streamlit.log
# หรือ
sudo journalctl -u streamlit-ai-grader -n 100
```

#### 2. ตรวจสอบ Port
```bash
sudo netstat -tlnp | grep 8501
# หรือ
sudo lsof -i :8501
```

#### 3. ตรวจสอบ Nginx
```bash
sudo nginx -t
sudo systemctl restart nginx
sudo tail -f /var/log/nginx/error.log
```

#### 4. ตรวจสอบ Firewall
```bash
sudo ufw status
sudo ufw allow 8501
```

#### 5. รันแบบทดสอบ
```bash
# รันแบบไม่ background เพื่อดู error
streamlit run Home.py --server.port=8501 --server.address=0.0.0.0
```

---

## 📞 ติดต่อขอความช่วยเหลือ

ถ้ายังแก้ไม่ได้ ให้รันคำสั่งนี้แล้วส่งผลลัพธ์มา:

```bash
echo "=== System Info ==="
uname -a
python3 --version

echo -e "\n=== Streamlit Process ==="
ps aux | grep streamlit

echo -e "\n=== Port Status ==="
sudo netstat -tlnp | grep 8501

echo -e "\n=== Files ==="
ls -lah /var/www/Project_AI_Grader/Home.py
ls -lah /var/www/Project_AI_Grader/pages/

echo -e "\n=== Recent Logs ==="
tail -50 streamlit.log

echo -e "\n=== Nginx Status ==="
sudo systemctl status nginx

echo -e "\n=== Service Status ==="
sudo systemctl list-units | grep streamlit
```

---

## ✅ สรุป

**ปัญหา:** Server รัน `student_view.py` (ไฟล์เก่า)  
**แก้ไข:** เปลี่ยนให้รัน `Home.py` (ไฟล์ใหม่)  
**วิธี:** ใช้สคริปต์ `update_streamlit.sh` หรือแก้ไขด้วยตัวเอง  
**ผลลัพธ์:** เว็บไซต์แสดงหน้า Home ใหม่ที่สวยงาม

---

**หมายเหตุ:** ไฟล์ `student_view.py` ยังอยู่ในระบบ ไม่ต้องลบ เพราะอาจมีการใช้งานในอนาคต
