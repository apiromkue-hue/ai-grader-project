# 🔧 วิธีแก้ไขปัญหา API Key รั่วไหล (Leaked)

## ⚠️ ปัญหา
API Key ของคุณถูก Google รายงานว่ารั่วไหลและถูกปิดการใช้งานแล้ว:
```
403 Your API key was reported as leaked. Please use another API key.
```

## ✅ วิธีแก้ไข (ใช้เวลา 2-3 นาที)

### ขั้นตอนที่ 1: สร้าง API Key ใหม่
1. **เปิดเว็บ Google AI Studio**: https://aistudio.google.com/apikey
2. คลิกปุ่ม **"Create API Key"** (สีฟ้า)
3. เลือก Google Cloud Project:
   - ถ้ามีอยู่แล้ว: เลือก project ที่ต้องการ
   - ถ้ายังไม่มี: คลิก **"Create new project"**
4. รอสักครู่ (5-10 วินาที) API Key จะถูกสร้าง
5. **คัดลอก API Key ที่ได้** (จะขึ้นต้นด้วย `AIzaSy...`)

### ขั้นตอนที่ 2: อัพเดท API Key ในโปรเจค
เปิดไฟล์ `.env` และแก้ไขบรรทัดนี้:
```bash
GOOGLE_API_KEY=YOUR_NEW_API_KEY_HERE
```

เปลี่ยนเป็น:
```bash
GOOGLE_API_KEY=AIzaSy...ของคุณ...
```

### ขั้นตอนที่ 3: รีสตาร์ท Streamlit
1. หยุด Streamlit ปัจจุบัน (กด `Ctrl+C` ใน Terminal หรือปิด PowerShell)
2. รันใหม่อีกครั้ง:
   ```bash
   .\run_streamlit.bat
   ```

### ขั้นตอนที่ 4: ทดสอบระบบ
1. เปิด browser: http://localhost:8501
2. คลิก "เข้าสู่ระบบนักเรียน"
3. Login ด้วย: `student1` / `student123`
4. อัปโหลดไฟล์ทดสอบ
5. ถ้าขึ้น ✅ "วิเคราะห์เสร็จสมบูรณ์!" = **สำเร็จ!** 🎉

---

## 🛡️ วิธีป้องกันไม่ให้ API Key รั่วไหลในอนาคต

### ✅ ควรทำ:
1. **ไม่อัพโหลด API Key ขึ้น GitHub** (เช็คว่า `.env` อยู่ใน `.gitignore`)
2. **ใช้ไฟล์ .env เสมอ** (อย่าเขียน hardcode ในโค้ด)
3. **ตั้งค่า API restrictions** ใน Google Cloud Console:
   - จำกัด IP address
   - จำกัด API ที่เรียกได้
4. **Rotate API Key ทุก 3-6 เดือน**

### ❌ ไม่ควรทำ:
1. ❌ อัพโหลดไฟล์ที่มี API Key ขึ้น GitHub public
2. ❌ แชร์ API Key ใน Discord, Facebook, หรือ social media
3. ❌ เขียน API Key ในโค้ดโดยตรง (hardcode)
4. ❌ คัดลอก API Key ไปวางใน website อื่น

---

## 📊 API Key Quota (Free Tier)
- **Requests per minute**: 15 ครั้ง
- **Requests per day**: 1,500 ครั้ง
- **Token limit**: 32,000 tokens per request
- **Cost**: ฟรี (Free tier)

---

## 🆘 ถ้ายังไม่ได้

### ปัญหา 1: "API key not valid"
**แก้:** 
- ตรวจสอบว่าคัดลอก API Key ครบทั้งหมด (ไม่มีช่องว่างหรืออักขระพิเศษ)
- API Key ต้องขึ้นต้นด้วย `AIzaSy`

### ปัญหา 2: "RESOURCE_EXHAUSTED"
**แก้:**
- รอ 1 นาที แล้วลองใหม่ (เกิน quota 15 requests/minute)
- หรือรอ 24 ชั่วโมง (เกิน 1,500 requests/day)

### ปัญหา 3: ยังขึ้น "403 leaked"
**แก้:**
- ต้องสร้าง API Key **ใหม่ล่าสุด** (ไม่ใช่ใช้ของเก่า)
- ตรวจสอบว่าไม่มีไฟล์อื่นที่ใช้ API Key เก่าอยู่

---

## 📞 ติดต่อสอบถาม
- **Email**: apeerom.koongkok@example.com
- **โรงเรียน**: เตรียมอุดมศึกษาภาคใต้

---

**อัพเดทล่าสุด**: 5 มกราคม 2026
**ผู้จัดทำ**: นางอภิรมย์ กึกก้อง (ครูชำนาญการพิเศษ)
